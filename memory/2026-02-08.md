# 2026-02-08

- 用户要求：后续所有 GitHub 项目都在 `zhoujun.sandbar` 身份下 clone/commit/push。
- 已将 `/root/wali_memory` 与 `/root/ai_tools` 同步迁移到 `/data00/home/zhoujun.sandbar/{wali_memory,ai_tools}`，并保留 .git，归属设为 zhoujun.sandbar。
- 已为 `zhoujun.sandbar` 生成 SSH key：~/.ssh/id_ed25519（公钥已提供给用户配置 GitHub）。
- 已验证 SSH 登录 GitHub 成功（Hi sandbarTrue），并将 `wali_memory`、`ai_tools` 的 origin 远程从 HTTPS 切换为 SSH：`git@github.com:sandbarTrue/{repo}.git`。
- 安全提醒：不要在聊天里发送 GitHub Token；如已泄露应立即 revoke。
