# 搞钱看板 v4 — 重新设计方案

## 一、核心概念定义

整个看板围绕 **4 个核心实体**，层级清晰不混淆：

### 1. 🧠 Session（会话）
- **定义**：OpenClaw 的一个持续对话上下文
- **类型**：主会话(main)、群聊(group)、私聊(dm)、Cron定时、子Agent(subagent)
- **来源**：`sessions.json` 映射表 + `.jsonl` 文件
- **展示**：名称、类型标签、最后活跃时间、正在执行的操作

### 2. 📋 任务（Task）
- **定义**：搞钱大王下达的一个业务目标
- **来源**：`TASK.md`（当前任务）+ `memory/YYYY-MM-DD.md`（历史任务）
- **状态**：待办 → 进行中 → 已完成 / 失败
- **关联**：每个任务关联到执行它的 session/screen/子agent

### 3. 🖥️ Screen 进程（Claude Code 后台）
- **定义**：通过 openspec-bg 派发到 screen 后台运行的 Claude Code 任务
- **来源**：`screen -ls` + `/tmp/openspec-bg-logs/*.log`
- **展示**：任务描述（从 prompt.txt 读取）、运行时间、模型、状态、费用
- **关联**：属于某个"任务"的子执行单元

### 4. 🤖 子 Agent（Subagent Session）
- **定义**：通过 `sessions_spawn` 创建的隔离子会话
- **来源**：`sessions_list` 中 kind=subagent 的 session
- **展示**：任务描述、模型、状态、运行时间
- **关联**：由主会话或群聊 session 派发

---

## 二、页面重新设计

### 首页 - 总览仪表盘

```
┌─────────────────────────────────────────────────┐
│ 🧠 AI Agent 实时状态                              │
│ ┌─────────┐ ┌──────────┐ ┌──────────┐ ┌───────┐ │
│ │ 活跃     │ │ 今日任务  │ │ 今日费用  │ │ 系统  │ │
│ │ Session  │ │ 完成/总数 │ │ $XX.XX   │ │ 状态  │ │
│ │ X 个     │ │ X/X      │ │          │ │ ✅/⚠️ │ │
│ └─────────┘ └──────────┘ └──────────┘ └───────┘ │
├─────────────────────────────────────────────────┤
│ 📡 正在执行的工作（实时）                          │
│ ┌─────────────────────────────────────────────┐ │
│ │ 🟢 [主会话] 瓦力(Opus) — 看板巡检修复         │ │
│ │    最近: $ npx next build (2分钟前)           │ │
│ │ 🟢 [搞钱大群] 瓦力(Opus) — 响应搞钱大王       │ │
│ │    最近: 发送飞书消息 (刚刚)                   │ │
│ │ 🔵 [Screen] direct-1771394142 — PainRadar    │ │
│ │    后端改造 (GLM-5, 运行 15 分钟)             │ │
│ │ 🟣 [子Agent] GLM-5 — 数据分析 (5分钟前完成)   │ │
│ └─────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────┤
│ 📋 当前任务进度                                    │
│ [从 TASK.md 读取当前活跃任务的 phase 进度]         │
│ ████████████░░░ 75% (6/8)                        │
│ 点击查看详情 → 跳转到任务页                        │
├─────────────────────────────────────────────────┤
│ 📊 模型用量排行 (前5)      💰 费用统计             │
│ [简洁表格]                  今日/本周/本月          │
└─────────────────────────────────────────────────┘
```

**关键改变：**
- "正在执行的工作"统一展示所有类型（session + screen + 子agent），不再分散
- 每个条目都有**可读的任务描述**，不是 UUID 或命令片段
- 去掉"待办队列"（那是 memory 文件里的笔记，不是系统任务）
- 去掉"任务统计 22 个"（那个数字不准确，混了太多来源）

### 任务页 - 任务看板

```
┌────────────────────────────────────────────────┐
│ [实时] [历史]                                    │
├────────────────────────────────────────────────┤
│ 📋 当前任务                                      │
│ ┌────────────────────────────────────────────┐ │
│ │ 🔧 看板信息质量优化                          │ │
│ │ 状态: ✅ 完成 | 创建: 13:12                  │ │
│ │ ┌──────────────────────────────────────┐   │ │
│ │ │ ✅ 后端修复 (3/3)                     │   │ │
│ │ │   ✅ extractAction 重写               │   │ │
│ │ │   ✅ 操作去重                          │   │ │
│ │ │   ✅ openspecHistory 字段              │   │ │
│ │ │ ✅ 前端优化 (4/4)                     │   │ │
│ │ │   ✅ 查看详情跳转                      │   │ │
│ │ │   ✅ OpenSpec 记录区块                 │   │ │
│ │ │   ✅ 视觉区分                          │   │ │
│ │ │   ✅ build 验证                        │   │ │
│ │ └──────────────────────────────────────┘   │ │
│ └────────────────────────────────────────────┘ │
├────────────────────────────────────────────────┤
│ 🖥️ Claude Code 执行记录                         │
│ ┌────────────────────────────────────────────┐ │
│ │ ✅ v3-info-upgrade | GLM-5 | 7m24s | $0.95│ │
│ │    任务: 前端信息质量优化 4 项              │ │
│ │    结果: 4/4 完成                          │ │
│ │ ✅ v3-realtime-upgrade | GLM-5 | 10m | $1.57│ │
│ │    任务: 实时性 + 进度条 7 项              │ │
│ │    结果: 7/7 完成                          │ │
│ │ ❌ painradar-backend | GLM-5 | 3m | $0.12 │ │
│ │    任务: 新闻分析修复                       │ │
│ │    失败原因: API 超时                       │ │
│ └────────────────────────────────────────────┘ │
├────────────────────────────────────────────────┤
│ 🤖 子 Agent 会话                                 │
│ [列表：label, model, 任务描述, 状态, 运行时间]    │
└────────────────────────────────────────────────┘
```

**关键改变：**
- 当前任务直接展示 TASK.md 的 checkbox 树（不是混合来源的 22 个）
- Claude Code 执行记录有**任务描述**和**失败原因**
- 子 Agent 独立区块
- 去掉"活跃 Session"表格（移到首页统一展示）

---

## 三、数据源重新规划

### stats.json 新结构

```json
{
  "wali_status": {
    "currentTask": "看板巡检修复",
    "status": "working",
    "executor": "瓦力(Opus)",
    "activeWork": [
      {
        "type": "session",
        "kind": "main",
        "label": "主会话",
        "executor": "瓦力(Opus)",
        "lastAction": "$ npx next build",
        "lastActiveMinutes": 2,
        "status": "active"
      },
      {
        "type": "session",
        "kind": "group",
        "label": "搞钱大群",
        "executor": "瓦力(Opus)",
        "lastAction": "响应消息",
        "lastActiveMinutes": 0,
        "status": "active"
      },
      {
        "type": "screen",
        "name": "direct-1771394142",
        "project": "painradar-backend",
        "taskDescription": "新闻分析修复 + /chat 追问 API",
        "model": "GLM-5",
        "runningMinutes": 15,
        "status": "running"
      },
      {
        "type": "subagent",
        "label": "GLM-5 数据分析",
        "model": "GLM-5",
        "status": "completed",
        "lastActiveMinutes": 5
      }
    ],
    "taskProgress": { ... },
    "recentActions": [ ... ],
    "openspecHistory": [
      {
        "session": "...",
        "project": "wali-dashboard",
        "taskDescription": "前端信息质量优化",
        "model": "GLM-5",
        "status": "success",
        "tasksCompleted": 4,
        "tasksTotal": 4,
        "durationMs": 444000,
        "cost": 0.95,
        "failReason": null,
        "completedAt": "2026-02-18T13:23"
      }
    ]
  },
  "models": { ... },
  "live_sessions": [ ... ]
}
```

**关键改变：**
- `activeWork` 统一数组：session + screen + subagent 混排，按活跃度排序
- screen 进程读取 prompt.txt 获取 **taskDescription**（不再只显示 session 名）
- openspecHistory 增加 **taskDescription** 和 **failReason**
- 去掉不准确的"任务统计 22 个"

---

## 四、后端改造清单

1. **wali-status.js** — `activeWork` 统一数组
   - Screen 进程：读 prompt.txt 第一行作为 taskDescription
   - 子 Agent：从 sessions.json 中 kind=subagent 的 session 收集
   - Session：已有的 live_sessions 合入

2. **openclaw-sessions.js** — openspecHistory 增强
   - 读 log 中的 result.result 字段提取失败原因
   - 读 prompt.txt 提取 taskDescription
   - 按时间倒序，最近 20 条

3. **去掉 parseTodoItems** — 不再从 memory 读待办（不准确）
   - 任务只来自 TASK.md
   - 待办队列数据删除

4. **recentActions 进一步优化**
   - `python3 -c "..."` → 提取 -c 后面的实际 python 代码用途
   - 长命令截断到 50 字符 + 省略号

---

## 五、前端改造清单

1. **首页重构**
   - "正在执行的工作"统一卡片：type 标签 + 任务描述 + 时间
   - 去掉"待办队列"区块
   - 去掉"任务统计 22 个"
   - 任务进度只展示 TASK.md 当前任务

2. **任务页重构**
   - 实时 tab：当前 TASK.md 任务 checkbox 树 + Claude Code 执行记录 + 子Agent
   - 历史 tab：openspecHistory 列表，每条可展开看详情
   - Claude Code 失败条目展示失败原因

3. **去掉混乱的概念**
   - 不再有"活跃 Session"和"Screen 进程"分开的区块
   - 统一成"正在执行的工作"
   - Session 统计改为"Session 分布"饼图（main/group/dm/cron/subagent）

---

## 六、执行计划

| 阶段 | 内容 | 执行者 | 预计时间 |
|------|------|--------|---------|
| 1 | 后端 wali-status + openclaw-sessions 改造 | 瓦力直接改 | 15min |
| 2 | 前端首页 + 任务页重构 | OpenSpec(GLM-5) | 15min |
| 3 | 验收测试（curl + 截图 + 数据验证） | 瓦力 | 10min |
| 4 | 部署 + 验收文档 | 瓦力 | 5min |
