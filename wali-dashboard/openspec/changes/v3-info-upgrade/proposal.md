# 看板信息质量优化

## 三个问题
1. "执行命令"看不出在干啥 — recentActions 的 action 描述太笼统
2. 完成的7个任务没法跳转看详情（提案/任务明细/验收结果）
3. 最近操作没有信息增量，都是重复的"执行命令"

## 根因
- 后端 wali-status provider 提取 action 时，对 exec 工具调用只显示"执行命令"，没有提取命令内容
- recentActions 没有去重，同一分钟内多个"执行命令"全部展示
- 前端任务进度没有链接到任务详情页
